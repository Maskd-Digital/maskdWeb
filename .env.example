# Gmail: use an App Password (Google Account → Security → 2-Step Verification → App passwords).
# Same mailbox can send to CONTACT_EMAIL_TO.

SMTP_HOST=smtp.gmail.com
SMTP_PORT=465
SMTP_SECURE=true

SMTP_USER=
SMTP_PASS=

# Where contact form submissions are delivered
CONTACT_EMAIL_TO=
